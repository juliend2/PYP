TODO :

-Regler le probleme de concatenation de string pour PHP. "." au lieu de "+" qui
est l'operateur d'addition
-Faire aussi que ce soit possible de faire des .= au lieu de += pour concatener les
strings
*** `N'EXISTE PAS EN PYTHON -permettre d'acceder a des attributs de classe comme
parent::ParentClassMethod()
-permettre ce truc : =&
-permettre les super-globales ($_GET, $_POST, $_REQUEST, etc) parce que sinon
elles sont parsees en CONSTANTE car leur nom est [_A-Z]+
-parser les quelques methodes cool du python dans leur equivalent en fonction
php. Genre :
    * ', '.join(list) deviandrait implode(', ', $list) 
    * str.split('-', 1) deviendrait explode('-', $str, 1)
    * list.append('element') deviendrait array_push($list, 'element')
-faire un parsing de fichiers de templates dans lesquels on peut mettre du
python ainsi que du php et html. Les balises seraient <?pyp .. ?> et
l'extension serait .pyp

Nice to have:

-permettre des commentaire # qui deviennent des //
-$this->{'nom de propriete'}->methode();
-un setting pour determiner la version de PHP que l'on compile. Si c'est 4 ou 5

